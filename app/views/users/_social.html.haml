%div
  %div.active
    - if current_user.nil? 
      %a{href: "/#{@user.username}/follow"}
        %p Follow
        %p= @user.username
    - elsif current_user.follow? @user
      %a{href: "/#{@user.username}/unfollow", "data-remote" => true}
        %p Unfollow
        %p= @user.username
    -else
      %a{href: "/#{@user.username}/follow", "data-remote" => true}
        %p Follow
        %p= @user.username

  %div
    %p
      %a{href: "#"} #{@user.followers.count}
    %p
      %a{href: "#"} Followers
  %div
    %p
      %a{href: "#"} #{@user.followings.count}
    %p
      %a{href: "#"} Following
