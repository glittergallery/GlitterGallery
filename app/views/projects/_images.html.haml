%ul.images
  - @images.each do |image|
    %li
      - #%img{:src => image.thumbnail('imagepath')}
      - # current problem - the file isn't showing.
      - # inspect source, fix, check in.
      = link_to (image_tag image.gsub("public","")), (File.join @project.id.to_s, 'masterbranch', File.basename(image))
    
    